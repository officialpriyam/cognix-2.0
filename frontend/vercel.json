{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "if git diff --quiet HEAD^ HEAD -- .; then echo '🛑 Skipping: no frontend changes'; exit 0; else echo '✅ Frontend changes detected — building'; exit 1; fi"
}
