# Allow newer Node versions than specified in engines
# This suppresses the "Unsupported engine" warning for Node 24+
ignore-engines=true

# Allow pnpm to hoist dependencies for better compatibility
public-hoist-pattern[]=*

# Enable shamefully-hoist for better compatibility with some packages
shamefully-hoist=true
